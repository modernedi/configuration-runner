# Synthetic public certificates

These self-signed certificates identify only ModernEDI SDK examples. They are
used to test PEM normalization, shared Production/Test references, and rotation.
The corresponding private keys were generated in memory and discarded. These
certificates cannot be used to configure a working AS2 connection.
